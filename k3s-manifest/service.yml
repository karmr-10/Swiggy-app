apiVersion: v1
kind: Service
metadata:
  name: swiggy-app
  namespace: karthik
  labels:
    app: swiggy-app
spec:
  selector:
    app: swiggy-app
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30080
  type: NodePort
  


