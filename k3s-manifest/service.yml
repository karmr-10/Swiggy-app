apiVersion: v1
kind: Service
metadata:
  name: swiggy-metadata-service
  namespace: karthik
  labels:
    app: swiggy-testapp
spec:
  selector:
    app: swiggy-testapp
  ports:
  - protocol: TCP
    port: 80  ## service port number
    targetPort: 5000 ## application port number
  type: LoadBalancer
  


